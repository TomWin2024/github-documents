# GitHub Pages Site

This repository is set up for GitHub Pages publishing using the `docs/` directory.

- Site source: `main` branch, `docs/` folder
- Published page: `docs/index.md`

如果要发布到 GitHub Pages，请将此仓库推送到 GitHub 并在仓库设置中将 Pages 源设置为 `main` 分支的 `docs/` 文件夹。